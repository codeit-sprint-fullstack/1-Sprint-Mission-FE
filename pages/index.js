import Head from 'next/head';
import Image from 'next/image';
import styles from '@/styles/Home.module.css';

export default function Home() {
  // 이 페이지는 header component만 배치하고 디테일은 나중에 추가한다.

  return <h1>홈페이지</h1>;
}
