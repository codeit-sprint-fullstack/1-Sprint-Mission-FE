{
  "compilerOptions": {
    "baseUrl": ".", // 프로젝트 루트를 기준 경로로 설정
    "paths": {
      "@/*": ["./*"] // @/로 시작하는 경로를 현재 프로젝트의 루트 디렉토리로 설정
    }
  },
  "exclude": ["node_modules", ".next"] // 빌드할 때 제외할 폴더 설정
}
